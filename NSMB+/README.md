## How to Use

1. Install dependencies: `pip3 install ndspy Pillow`
2. Place your own NSMB DS ROM inside the `NSMB+` folder
3. Run: `python3 scripts/swap.py your_rom.nds NSMB+.nds`
4. Load `NSMB+.nds` in your emulator or flash it to a cartridge